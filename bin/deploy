#!/usr/bin/env sh

set -e

color_off='\033[0m'
cyan='\033[0;36m'

echo "${cyan}Make sure you build and commit before deploying${color_off}"

git push

ssh sunfox 'cd /var/www/onironautes.fr/ && git pull'
